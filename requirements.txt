# Core dependencies
gradio>=6.0.0
python-dotenv
mcp

# Blaxel SDK (with all extras for sandbox, agents, MCP)
blaxel[all]

# Data visualization
plotly>=5.17.0

# OpenTelemetry (observability)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0

# Additional utilities
httpx
pydantic
